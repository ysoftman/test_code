apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-ysoftman.fullname" . }}
  labels:
    {{- include "nginx-ysoftman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-ysoftman.selectorLabels" . | nindent 4 }}
